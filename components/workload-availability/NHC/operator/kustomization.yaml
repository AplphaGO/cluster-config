apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
 - ./subscription.yaml


patches:
  - target:
      kind: Subscription
      name: node-healthcheck-operator
    path: patch-channel.yaml

#resources:
#  - https://github.com/redhat-cop/gitops-catalog/node-healthcheck-operator/operator/overlays/stable?ref=ma